import { DateTripFilterPipe } from './date-trip-filter.pipe';

describe('DateTripFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DateTripFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
